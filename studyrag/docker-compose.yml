version: '3.8'

services:
  studyson:
    build: .
    ports:
      - "7860:7860"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - HOST=0.0.0.0
      - PORT=7860
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
